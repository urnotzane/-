<!-- AUTHOR @ ZANE -->

<view class="rate">
  <icon bindtap='_goodTap' class='iconfont icon-rate' data-index="{{index}}" style="color: {{item?'#33d1bf':''}}" wx:for="{{rate}}" wx:key="{{index}}"></icon>
</view>