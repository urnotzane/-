<!-- AUTHOR @ ZANE -->

<view class="rate">
  <icon bindtap='_goodTap' class='iconfont icon-rate' data-index="{{index}}" style="color: {{item?'#FCAC00':''}}" wx:for="{{rate}}" wx:key="{{index}}"></icon>
</view>