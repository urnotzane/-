<block wx:if="{{type=='page'}}">
  <PageHeader title="{{title}}" url="{{url}}" className="{{className}}" />
</block>

<block wx:if="{{type=='section'}}">
  <SectionHeader title="{{title}}" url="{{url}}" border="{{border}}" />
</block>