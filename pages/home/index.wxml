<view class="navs">
  <view class='title'>首页</view>
</view>

<view class="container">
  <Accordion wx:for="{{list}}" wx:key="{{index}}" data="{{item}}">
    <view slot="main">
      <List type="base" data="{{item.detailList}}" />
    </view>
  </Accordion>
</view>